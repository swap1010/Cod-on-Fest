class Solution:
    def fib(self, n: int) -> int:
        if n < 2:
            return n
        a = 0
        b = 1
        while n > 1:
            n -= 1
            a, b = b, a+b
        return b
            
